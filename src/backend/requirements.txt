django # Das Backend-Framework — ohne das läuft nichts
PyMySQL # Damit Django mit MariaDB sprechen kann
djangorestframework # Macht Django API-fähig — Frontend kann Daten abrufen
django-cors-headers # Erlaubt React Frontend auf Django zuzugreifen — ohne das blockiert der Browser alle Anfragen